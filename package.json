{
  "name": "simplydark",
  "version": "1.0.0",
  "description": "A simple dark mode Chrome extension",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "dev": "tsc --watch & nodemon --watch manifest.json --exec 'ts-node build.ts'"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.260",
    "@types/node": "^20.0.0",
    "nodemon": "^3.0.0",
    "ts-node": "^10.9.0",
    "typescript": "^5.0.0"
  }
}
